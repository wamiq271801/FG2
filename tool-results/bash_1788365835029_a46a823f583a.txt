Task ID: R1 (rollback)
Agent: main-agent (Z.ai Code)
Task: User request — "Delete the latest version and restore the version V15": roll back the entire Next.js 16 Cache Components migration (latest session work) and restore the previous stable version.

Work Log:
- Investigated version references: no git tags; 18 commits; latest commit 5e2fa02 had been auto-amended by the platform to contain the in-progress Cache Components migration (31 files: cacheComponents config, 'use cache' aggregates, CirculationSection restructure, NewBadge island, ref-counted nav loader, sitemap cache helpers).
- Identified 1ebaea8 ("refactor(routes): organize sitemap routes under app/(sitemaps) route group") as the stable pre-migration version: ISR revalidate configs intact, tracking system intact (services/tracking.ts + trackers), sitemap system complete, original Link/NavigationProgress/ProductCard.
- Backed up the user's uploaded task spec to /tmp, then `git reset --hard 1ebaea8` — deleted the latest version (migration commit) and restored the tree; re-placed the upload file afterwards.
- Verified restored state: next.config.ts has NO cacheComponents; `export const revalidate = 300/3600` present across pages/sitemap routes; NewBadge.tsx / CirculationGrid.tsx removed; getTrendingProducts/getFeaturedProducts/getNewArrivals/getOnSaleProducts restored; Link.tsx back to boolean nav-progress store.
- Restarted dev server (:3000) and TrackingServer (:3001, bunx tsx watch).
- Browser verification: /search renders with product cards and ZERO client-side exceptions (the migration-era async-Server-component-in-client-tree error is gone); first-click shop→product navigation commits; no console errors.
- Content verification: PDP H1 + JSON-LD graph (WebPage/BreadcrumbList/Product) + canonical + 8 product links; home 32 product + 33 category links + JSON-LD; NEW badges render (original 60-day rule, as in V15); tracking POST /events fires from browser (200 OK, no_tidb sandbox warning expected).

Stage Summary:
- Repository restored to version 1ebaea8 (pre-migration stable state); the Cache Components migration is fully removed. Next.js remains 16.1.3 with ISR (revalidate) caching, original navigation/loader code, original 60-day NEW badge behavior, and the complete tracking + sitemap + SEO systems. All routes verified working.

---
Task ID: 1
Agent: main (Z.ai Code)
Task: UI improvements — (A) centralized browser-history back arrow in the shared breadcrumbs, (B) CSS single-line breadcrumb truncation, (C) mobile-only header loading indicator visibility bump.

Work Log:
- Inspected: components/shared/Breadcrumbs.tsx (Server Component, sole consumer of ui/breadcrumb primitives, used by 16 pages), components/ui/breadcrumb.tsx, components/layout/NavigationProgress.tsx (the header loading indicator: fixed top 2px comet bar), components/layout/SiteHeader.tsx, hooks/use-mobile.ts (768px mobile breakpoint), tailwind-merge behavior verified empirically for class overrides.
- NEW components/shared/BreadcrumbBackButton.tsx — minimal "use client" island: ghost icon Button (size-7 override, matching sidebar.tsx compact convention), lucide ArrowLeft size-3.5 (matches separator scale), aria-label="Go back", onClick → window.history.back(). No hardcoded/inferred destination, no custom history.
- EDIT components/shared/Breadcrumbs.tsx — stays a Server Component; nav gains flex items-center gap-1.5; back button rendered before the trail (outside the <ol>, not a breadcrumb item); BreadcrumbList gets min-w-0 flex-1 flex-nowrap overflow-hidden whitespace-nowrap; last item gets min-w-0 + truncate. No changes to crumb data/URLs.
- EDIT components/layout/NavigationProgress.tsx — bar height h-[2px] max-md:h-[2.4px] (+20% below 768px only). Animation (nav-progress 1.2s), gradient color, desktop/tablet sizing untouched.
- Validation: eslint + tsc — zero errors in changed files (all reported errors pre-existing in unrelated files: scripts/*.js require-imports, ContactForm zod overloads, ProcessingServer/skills modules). Documented baseline, left untouched.
- Browser verification (agent-browser, dev server restarted via .zscripts/dev.sh): back arrow first child of nav, aria-label "Go back", no visible text; real history back proven (shop→home→product: arrow → / ; shop→product: arrow → /shop — destination follows actual history, never hardcoded); mobile 375px single 28px row, ellipsis truncation engaged on long product name, arrow visible, no viewport overflow; loader heights measured live during navigation: 1280px→2px, 768px→2px, 375px→2.39px (Chrome 1/64px snap of 2.4px); homepage has no back arrow/breadcrumbs (unchanged); zero console/page errors; keyboard focus works.

Stage Summary:
- 3 files total (2 edited, 1 new minimal client island). Breadcrumb data generation, URLs, routing, all other systems untouched. Desktop appearance unchanged except the compact arrow; tablet loader unchanged; mobile loader mildly (~20%) more prominent.

---
Task ID: 2 (Phases A/B/C)
Agent: main (Z.ai Code)
Task: Bounded Supabase performance refactor — A: PDP brand/category consolidation, B: Offers promotion product batching + card projection, C: Home circulation batching. Review-summary and Shop explicitly out of scope, untouched.

Work Log:
- Inspected all catalog modules, 3 target pages, ProductCard, schema DDL (brands/categories NOT NULL), schema builders, and every caller of the affected functions (getBrandBySlug: PDP-only; getProductsByIds: offers-only; the 4 circulation fns have other callers in search/cart/wishlist/offers).
- PHASE A: PRODUCT_DETAIL_SELECT brands!inner(slug,name,country) + categories!inner(id,slug,name) (country verified REQUIRED — PDP renders brand.country visibly; audit was wrong); mapProductRow + Product type expose brandCountry/categoryName; PDP dropped getBrandBySlug/getCategoryBySlug calls; all 7 consumers (breadcrumb label+href, JSON-LD breadcrumb/category/brand, visible brand line, "More in") now read product fields. Functions kept (getCategoryBySlug used by category page).
- PHASE B: new getProductsByIdsCard (PRODUCT_CARD_SELECT, same id-order semantics); offers page batches featured+all promotion IDs (deduped) into ONE query, resolves sections via in-memory map; PromotionSection now pure (products prop). getAllCategories KEPT — live-data proof chip ordering derives from categories table order and differs from product-appearance order (smartwatch/flashlights swap), not reproducible from product data per task §5.5 escape clause.
- PHASE C: getCirculationIdsBatch (one query for all 4 surfaces, PGRST205→all-empty, per-surface position sort + limit slice); the 4 single-surface functions' fallback bodies extracted verbatim into shared private helpers (zero behavior change, other callers unaffected); new getHomeCirculationSections (1 circulation + 1 product query for unique IDs; per-surface fallback only when that surface is empty); home page calls it once with limits {4,4,4,4}.
- VERIFICATION (temp stderr probe on catalog client, removed after): PDP 9→7 queries (brands+categories lookups gone; footer categories query is pre-existing, out of scope); offers 7→5 with populated promotions (featured+2 promo-section product queries → 1 batched card query); home 10→7 on current DB (4 circulation→1; 4 fallbacks preserved) and populated-path mock test proved 8→2 (1 circulation + 1 products) with 12/12 assertions: exact query counts, per-surface order/membership equivalence vs old path, limit slicing, shared-product dedup, card-field integrity.
- HTML equivalence via git stash round-trips (scripts stripped, JSON-LD compared): /offers populated 108,210 chars IDENTICAL; /offers empty-path IDENTICAL; / IDENTICAL (188,485); PDP IDENTICAL (48,135). JSON-LD identical on all.
- Test-fixture hygiene: 3 temp promotions + 11 offer_products inserted via service role for populated-path testing, then DELETED; DB verified back to empty offers/offer_products. circulation_entries cannot be DDL-created via PostgREST → populated circulation path verified via bun mock-module test of the real functions instead.
- tsc: 4 pre-existing errors only (ContactForm zod, ProcessingServer, skills/) — baseline unchanged. eslint: clean on all affected files. Browser: all 3 pages render, zero console/page errors.

Stage Summary:
- 6 files changed (+248/−60): 3 pages, products.ts, catalog types.ts, types/index.ts. Requests removed: PDP −2 (brand+category lookups), offers N-promo product queries → 1 batched card-level query (no detail-field overfetch for ProductCard), home 4 circulation→1 (+ 4 product→1 on populated path). getAllCategories retained on offers (chip ordering not derivable — live-data evidence). All fallbacks, limits, ordering, rotation seeds, ISR (revalidate 300), React.cache, tracking, JSON-LD, SSR semantics preserved and verified.

---
Task ID: 3 (Cache Components migration, re-do)
Agent: main (Z.ai Code)
Task: End-to-end modernization per uploaded spec (Pasted Content_1787997500607.txt): Next.js 16 Cache Components architecture, page-level cacheLife profiles, 7-day NEW badge, first-click navigation, loader lifecycle, SEO preservation — redoing (correctly) the migration that was previously rolled back (5e2fa02 → reset to 1ebaea8).

Work Log:
- PHASE 1 RESEARCH (official docs, via browser): next-prerender-current-time (Date.now before uncached/request data = error; inside 'use cache' = allowed/baked; Client Component for per-request display), useLinkStatus (pending = click→history-update only; prefetch SKIPS pending; docs bless delayed-reveal), cacheLife next.config (custom profiles syntax), migrating-to-cache-components (segment configs error after enabling; generateStaticParams ≥1 param; params awaited inside Suspense; GET handlers via cached helpers; generateMetadata follows component rules; Activity state preservation), ISR-with-cache-components (prerender subset + cached data helpers + Suspense pattern), partialPrefetching (16.3.0+ ONLY — NOT available on pinned 16.1.3; skipping it, staying on 16.1.3 because 16.3.3 breaks the preview gateway per documented history).
- PHASE 2-3 BASELINE: production build of V15 code (53 pages; home/offers/categories static 5m; shop/product/category/search dynamic ƒ). next start + full navigation matrix via agent-browser: first click 378ms commit, all flows pass, loader lifecycle correct (visible 150-361ms), prefetch never flashes loader. The "stuck → second click" bug does NOT reproduce in V15 baseline (migration-era artifact + cold-render latency, GitHub #57565 class).
- PHASE 5-8 IMPLEMENTATION: next.config cacheComponents:true + cacheLife profiles home(300/300/3600), product(300/900/3600), catalog(300/300/3600), sitemap(3600/3600/86400). Home/offers/categories-index/cart/wishlist → page-level 'use cache' components (public data only; cart/wishlist content lives in client islands). PDP → generateStaticParams (all 28 slugs; prerendered FULL pages) + cached aggregate getProductPageData('product' profile; product+related+reviews+variation+offers; metadata shares entry) + Suspense(params) + RelatedProductsGrid cache component (badge Date.now baked). Category [slug] → cached aggregate (category+products+others; metadata shares) + Suspense(params) + CategoryResults receives data as props (searchParams-first render). getProductBySlug → 'use cache'('product') replacing React.cache (shared by PDP+3 review routes). Reviews pages → Suspense(params) + cached getProductBySlug. Search → static shell + Suspense(content) + connection() dynamic marker (searchParams-metadata). Sitemaps → cached helpers ('sitemap' profile); static sitemap just drops revalidate. SiteFooter → cache component (catalog profile; year + categories baked). RouteGuard wrapped in Suspense in root layout (usePathname suspends during generic app-shell prerender of non-enumerated dynamic routes — reviews/orders). ProductCard NEW badge: 60 days → EXACTLY 7 days (isNewProduct extracted pure fn; server=baked at cache-fill, client=viewer clock).
- PHASE 10-12 NAVIGATION/LOADER: use-nav-progress → reference-counted store (start/complete/reset, clamp at 0); LinkStatusReporter → startedRef-guarded (only completes what it started; unmount cleanup releases count); NavigationProgress → visual UI byte-identical (incl. 2px/max-md 2.4px bar, 120ms reveal, comet animation), reads pendingCount, pathname-change reset as safety net. Prefetch verified NOT to trigger loader (6s observation, 0 flashes).
- BUILD ITERATION: (1) Date.now in ProductCard during PDP prerender (cached-data-only scope) → RelatedProductsGrid cache component; (2) usePathname suspension blocking shells for non-enumerated dynamic routes → RouteGuard Suspense; then BUILD SUCCEEDED: 87/87 pages; /product/[slug] ◐ PPR with 29 paths; /categories/[slug] ◐ with 25; shop/search/reviews/orders ◐ PPR; home/offers/cart/wishlist/categories ○ static 5m/1h; sitemaps ○ 1h/1d.
- PHASE 18-19 VERIFICATION (production runtime): first click /shop→product commits in 20ms (baseline 378ms) with no loader flash; different products, direct URL, shop→category→product, back/forward (Activity) all pass; concurrent-click race test passes (B commits, no stuck loader); loader slow-nav test: prerendered shell commits instantly, content streams post-commit (loader correctly completes at commit); on-demand slug (temp product): cold streams full content 423ms (warm connections) / warm 30ms; cache headers prove semantics: s-maxage=300 + SWR 3300 (=expire 3600) on home/offers/product, shop uncached; deleted product correctly serves stale 200 until revalidate window (SWR); SSR HTML: PDP H1/specs/reviews/JSON-LD/canonical/R2 img with alt+dimensions all present, shop 12 links+12 R2 images; Product JSON-LD fully valid (one graph, all fields, absolute R2 images); NEW badge in SSR HTML (shop: 10, PDP related: 3) for 3.4-day-old products; RSC responses 200 text/x-component; images HTTP/2 200 image/avif via R2 CDN (no proxying); sitemaps 200 with 28 URLs; 21-route smoke matrix all 200; mobile 375px: no overflow, breadcrumb single-line, back arrow, footer bottom.
- SECURITY: no SUPABASE_SERVICE_ROLE_KEY/SECRET_KEY in app code (only pre-existing config.ts declaration for Worker/ProcessingServer, unchanged); catalog client anon-only; RLS untouched.
- KNOWN ARTIFACT (framework, 16.1.x): cacheComponents amplifies Link prefetch duplication (~8 requests/route vs 2 on V15 — fixed only by 16.3+ partialPrefetching which is unavailable here); 1 silent CDP exception per page load (React internal Suspense promise control-flow during PPR hydration — fires at 0.7s before prefetches, never reaches window.onerror/console, zero functional impact; V15 live site has 0). Both documented as framework behavior, not app defects.
- REGRESSION TESTS: tests/regression.test.ts (bun test) — 9/9 pass: NEW badge exact 7-day boundaries (1h/6d/6d23h59m → NEW; exactly 7d/8d → NOT NEW; window = 604800000ms) + ref-counted nav store (concurrent counts, zero-clamp, reset).
- VALIDATION: production build ✓ (multiple full cycles); eslint clean on all 24 touched files; tsc = 4 pre-existing baseline errors only (tests/ excluded from tsconfig for bun:test types); bun test 9/9; dev server restored via .zscripts/dev.sh with dev-mode product page rendering clean (no validation overlay errors).
- Cleanup: temp on-demand probe products (2) inserted for App-Shell/streaming/cold-render tests then deleted (verified 0 remaining); debug instrumentation added and removed; DB back to original state.

Stage Summary:
- 24 files changed (+580/−198) + tests/regression.test.ts. Full Cache Components architecture on Next 16.1.3 without legacy segment configs; page-level cacheLife profiles exactly per spec (home 300/300/3600, product 300/900/3600); NEW badge exactly 7 days preserving SSR rendering; first-click navigation now instant (20ms) via build-time prerendering of all 28 products + 25 categories; loader race-safe ref-counted lifecycle with visual UI unchanged; all SEO surfaces verified in production output (links, R2 images, JSON-LD, canonical, metadata); Supabase anon/RLS architecture intact.

---
Task ID: P1
Agent: main-agent (Z.ai Code)
Task: Phase 1 per uploaded spec (upload/Pasted Content_1788289977794.txt): review-moderation readiness (status + customer_name on the EXISTING product_reviews table, approved-only public boundary via a published_reviews VIEW) + a completely separate private admin application (/admin, own package.json/deps/Next 16.1.3) for Products/Categories/Reviews, exposed through the sandbox dev URL, with the provided sb_secret_* test key as the admin's server-only privileged credential.

Work Log:
- PHASE 0 (read-only): read the full spec; inspected package.json, Caddyfile (project copy), .zscripts/dev.sh, gateway behavior (root-owned /app/Caddyfile on :81 proxies everything to :3000; /admin 404'd; XTransformPort query mechanism), Supabase clients (browser anon / server catalog anon / config.ts hardcodes URL+anon+legacy service_role for Worker+ProcessingServer), full review flow (modules/review/data.ts, ReviewGate, ReviewEditGate, ReviewForm, useReviewEligibility, RLS 07_rls.sql, triggers 06_triggers.sql, can_review_product), product/category schemas + data modules, live DB via PostgREST OpenAPI with the provided sb_secret key (75 products, 25 categories, 2 profiles, 0 orders, 0 reviews, user_id has NO default, live schema matches the repo snapshot + later password-reset objects).
- DDL access investigation (exhaustive): sb_secret key works for PostgREST DATA only; Management API v1 rejects it ("JWT could not be decoded" / "Unauthorized" / "JWT failed verification" for the legacy JWT); pg-meta/platform endpoints 404; direct pooler connection (project in aws-0-ap-northeast-1) with sb_secret or service_role JWT as password fails auth; no psql/supabase CLI; no DB password or PAT anywhere in the sandbox. CONCLUSION: DDL cannot be executed against the live test DB from this environment — the migration ships as a file and is the one pending manual step (SQL editor / MCP / PAT).
- MIGRATION (file): supabase/migrations/20260901180000_review_moderation_phase1.sql — review_status_enum; status NOT NULL DEFAULT 'pending'; customer_name text; user_id DEFAULT auth.uid() (fixes the pre-existing submission blocker: ReviewForm inserts without user_id and the live table has no default); stamp_review_moderation() SECURITY DEFINER trigger (customer INSERT → status forced 'pending' + customer_name stamped from profiles; customer UPDATE → status/customer_name preserved; privileged session passes through = admin approve/reject path); one-time grandfathering of pre-migration rows to 'approved' (preserves current public meaning; live table empty); published_reviews VIEW (approved-only, public columns only, no user_id/status); partial indexes; public_read policy becomes status='approved' OR owner; REVOKE base-table SELECT from anon + GRANT view SELECT to anon/authenticated; function grants for service_role (generate_sku, can_review_product, set_updated_at, stamp_review_moderation — none were granted, which blocks API-driven product INSERTs and review UPDATEs today); NOTIFY pgrst reload.
- Canonical schema snapshot updated to match: 01_types (+review_status_enum), 02_tables (+status/customer_name/user_id default), 04_indexes (2 partial indexes), 05_functions (+stamp_review_moderation), 06_triggers (+moderation trigger), 07_rls (new public_read policy), 08_grants (+service_role function grants, anon base-table revoke, view grants), NEW 09_views.sql, wipe_and_rebuild.sql references + drops.
- STOREFRONT (minimal): modules/review/data.ts public reads (getReviewSummary/getLatestReviews/getPaginatedReviews) now query published_reviews with customer_name as authorName (fallback "Verified buyer"), profiles join removed, getReviewById removed (edit flow moved to owner-scoped client fetch). Edit page drops the anon SSR review fetch; ReviewEditGate fetches id/user_id/rating/title/body with the browser client (RLS: owner sees own pending review; non-owner sees approved-only; ownership still the user_id check; edit still protected by the owner UPDATE policy). types/index.ts Review.userId now optional (public reviews carry no identity). Submission flow UNTOUCHED in code (the DB trigger stamps status+customer_name). Review UI components untouched (ReviewItem/ReviewList/ReviewActions/eligibility hook preserved).
- ADMIN APP (admin/, separate Next.js 16.1.3 app, basePath /admin, port 3001, own package.json/tsconfig/eslint/postcss/Tailwind v4): server-only privileged Supabase client (sb_secret key from git-ignored admin/.env.local; import 'server-only' guards); access boundary = password login (ADMIN_PASSWORD in .env.local, timing-safe compare) + HMAC-signed HttpOnly session cookie + (protected) route-group layout gate; every server action re-verifies the session. Products: list/search/pagination (live data), detail, create (SKU server-side fallback generator because generate_sku() is ungranted pre-migration), edit, images manager (add URL/set-primary/delete), archive/activate (is_active), hard delete (FK-aware errors). Categories: list with product counts, create, edit, delete guarded by RESTRICT reference check. Reviews: pending/approved/rejected/all tabs, moderation context columns, approve/reject actions, and an actionable "migration required" panel when the status column is missing (current live state).
- DEV ORCHESTRATION: root package.json dev → scripts/dev-orchestrator.mjs starts storefront (:3000, exact prior command piped to dev.log) + admin (:3001, admin-dev.log), idempotent port checks, first-run bun install for admin deps, crash auto-restart (verified live by killing the admin server), signal handling. .zscripts/dev.sh unchanged (runs bun run dev). Storefront next.config.ts gained ONLY an async rewrites() proxy: /admin and /admin/:path* → http://127.0.0.1:3001/admin/... (pure dev-gateway proxy; the root Caddy only forwards to :3000 and is root-owned; the storefront owns no admin route). Admin next.config: experimental.serverActions.allowedOrigins + allowedDevOrigins with localhost:81 and *.space-z.ai (Next's csrf-protection supports subdomain wildcards — verified against next@16.1.3's isCsrfOriginAllowed; the space-z.ai preview chain sets x-forwarded-host to an intermediate fcapp.run host that never matches the browser origin, which aborted Server Actions before the fix).
- FIXES DURING VERIFICATION: Pagination moved to a server component (function props can't cross the server→client boundary); GET search forms submit to the current URL (root-absolute action bypassed basePath); VISUAL_KEYS/types moved to client-safe lib/product-constants.ts (client form value-imported from a server-only data module); redirect() moved outside try/catch in both create actions (NEXT_REDIRECT was swallowed); reviews actions made async (non-async exports in "use server" modules produce zero exports); cn moved to lib/cn.ts (plain functions from "use client" modules aren't callable in server components); friendlyDbError handles plain PostgrestError objects.
- VERIFICATION (agent-browser through the :81 gateway): storefront matrix 200 (/, /shop, /categories, /search, /offers, /cart, PDP, reviews list/new/edit pages render the same states as before — reviews were empty and remain empty, so no visual delta); PDP "Customer reviews / No reviews yet" intact; edit gate states (unauth → sign-in prompt; non-owner/nonexistent → "You can't edit your review"). Admin end-to-end with live DB: login → products list (75 total, 20 rows, search "blender" → 1, page 2/4), product detail (49 fields, 3 images), CREATE product (redirect to detail + banner + SKU Q4VNIM9ZOF + DB row verified), EDIT (price 1999→2499 + story, DB verified), add image (DB verified), set primary (DB verified), archive → false → activate → true (DB verified), two-step-confirm DELETE (row + cascaded images gone, DB verified), categories list (25) + CREATE + EDIT (tagline/subcategories DB verified) + DELETE (0 refs, DB verified) + delete-guard panel on a referenced category (1 product, blocked). Reviews page renders the migration-required panel naming the migration file. Sign out clears the session. Mobile 375px no overflow. Admin routes 307 → /admin/login when unauthenticated. Hydration-error A/B test (git stash round-trip) proved the pre-existing AuthProvider/SiteHeader dev-mode mismatch is NOT from these changes. Secret-key audit: 0 occurrences in served HTML, all client chunks, both dev logs, no NEXT_PUBLIC vars, admin/.env.local git-ignored, storefront .env untouched; anon key INSERT product → 401, anon PATCH reviews → 400, anon reads profiles → permission denied.
- VALIDATION: bun test 9/9; storefront tsc = pre-existing baseline only (ProcessingServer/TrackingServer/ContactForm/skills); storefront lint = pre-existing baseline files only; admin tsc 0 errors + eslint clean; dev.log storefront error count 0 (pre-existing cross-origin dev warning from the preview gateway documented in worklog history).

Stage Summary:
- Delivered: migration file + canonical schema updates (DB DDL application is the single pending manual step — no SQL-execution path exists from this sandbox with the provided credential); storefront now reads approved-only public reviews through the published_reviews view with customer_name (no profiles dependency), submission/eligibility/ownership/edit behavior preserved; complete separate admin app at /admin (own deps, server-only secret key, password+cookie boundary) with working live Products/Categories CRUD and Reviews moderation UI (activates when the migration is applied); both apps start/restart under the updated dev script (scripts/dev-orchestrator.mjs via bun run dev / dev.sh) and /admin is reachable through the sandbox gateway via a storefront rewrite proxy; admin login password is in admin/.env.local (5d47c131bdc0).
- Key decisions: pre-migration rows grandfathered to 'approved' (preserves current public meaning); the published view is a definer-semantics VIEW with anon grants ONLY on the view (single public boundary); customer edits never change status (per spec — no invented approved→pending reset); admin SKU fallback generated server-side until the generate_sku grant ships with the migration; *.space-z.ai wildcard in serverActions.allowedOrigins for the preview chain (verified against Next's matcher).
- Phase 2 boundary respected: no revalidate architecture, no cache invalidation, no storefront cacheComponents/cacheLife changes, no Worker/TrackingServer/ProcessingServer modifications, no storefront UI changes (review rendering identical).

---
Task ID: 2 (Phases A+B)
Agent: main-agent (Z.ai Code)
Task: Phase 2 per uploaded spec (upload/Pasted Content_1788358950621.txt) — deep audit + target architecture design for the unified storefront-caching/admin-notification system.

Work Log:
- Read the full Phase 2 spec (2696 lines) and the complete worklog history (V15 restore → Cache Components migration → Phase 1 admin app).
- PHASE A audit findings: next.config has cacheComponents + 4 TTL cacheLife profiles (home/product/catalog/sitemap — the forbidden freshness mechanism); 14 'use cache' scopes inventoried (home page-level giant cache incl. feed+products, PDP aggregate incl. reviews, offers/categories/cart/wishlist page-level, SiteFooter, 3 sitemap helpers, getProductBySlug); stock is SELECTed into PRODUCT_CARD_SELECT/PRODUCT_DETAIL_SELECT and baked into every cached page (ProductCard badge/overlay, PDP AvailabilityBadge/PurchaseControls/VariationSelector, related-products query filters stock>0); shop/search dynamic; brands/offers/circulation have NO admin mutation paths (offers table + circulation_entries missing/empty live); admin calls revalidatePath("/", "layout") on itself (useless cross-app); sitemaps cached with TTL profiles; admin/.env.local was WIPED by the Sep 2 sandbox reset (recreated: sb_secret key lost → verified legacy service_role key works for PostgREST data API, restored as the privileged admin credential; admin password restored from worklog: 5d47c131bdc0); Phase 1 review-moderation migration still NOT applied to live DB (published_reviews 404, product_reviews has no status column) and DDL verified impossible from this sandbox (Management API + pooler both reject the service-role JWT).
- Next.js 16.1.3 semantics verified from installed source (node_modules/next/dist/server/use-cache/*): cacheLife accepts revalidate/expire: Infinity; NESTED 'use cache' scopes propagate inner tags to the outer entry (propagateCacheLifeAndTagsToRevalidateStore) — therefore cached scopes must never consume other cached scopes unless the dependency is intentional, and no page-level caches are kept on catalog pages (data-scope caching only).
- PHASE B design (docs/phase-2-architecture.md): pages become uncached server renders assembling tagged data scopes (product:{id}+category:{id} for detail, products+category:{id}×distinct for the card dataset, categories/category:{id}/category-slug:{slug}, feed:home returning IDs only with own selection queries, reviews:{productId}+reviews, offers/offer:{slug}, sitemap:products/sitemap:categories, footer tagged categories); stock removed from all cached selects + PDP live batched stock read (getStocks) + one hydration-time client fetch via GET /api/stock; event vocabulary product.*/category.*/review.* (+refresh variants); POST /api/revalidate endpoint with Bearer shared secret (REVALIDATE_SECRET); storefront-owned invalidation policy resolving events→tags (incl. previousSlug/old-category handling); admin notifier = one attempt, no queue, no auto-retry, explicit [Retry Invalidation].
- Generated REVALIDATE_SECRET (hex32) and wrote storefront .env/.env.local + admin/.env.local (SUPABASE_URL, service-role secret key, ADMIN_PASSWORD, ADMIN_SESSION_SECRET, STOREFRONT_REVALIDATE_URL/SECRET).

Stage Summary:
- Architecture finalized: ONE invalidation system owned by the storefront, admin as domain-event notifier, data-level cache granularity, no TTL freshness, stock out of cache, static/user/search pages preserved. Contracts (event vocabulary, endpoint, env keys, response shapes) frozen for Tasks 3+ (storefront rewrite by main agent; admin integration delegated with the frozen contract).

---
Task ID: 3
Agent: full-stack-developer
Task: Admin mutation-flow integration with the storefront cache-invalidation notification system (Phase 2 Phases F+G+H) — notifyStorefront after every successful DB mutation, explicit operator retry, and manual storefront revalidation controls, all inside /home/z/my-project/admin only.

Work Log:
- Read worklog (Task 2 / P1 entries), docs/phase-2-architecture.md, the full admin app (actions, data layers, forms, pages, ui.tsx, env.ts) and the frozen contract; verified zod 4.5.4 API surface (discriminatedUnion + strictObject + z.uuid, strict extra-key rejection) with a script before writing code.
- NEW admin/lib/notify-types.ts (client-safe): StorefrontEvent union (all 12 variants) + strict zod schema with a compile-time schema↔type assertion; isomorphic base64URL codec (btoa/atob + TextEncoder/TextDecoder — valid in Node 16+ and browsers) via encodeEvent/decodeEvent; withNotifyFailed()/readNotifyMessage() redirect-URL helpers. Base64URL alphabet sidesteps query-string "+"-decoding hazards; verified evt survives real Next searchParams round-trips.
- NEW admin/lib/notify.ts (server-only): notifyStorefront(event) — exactly ONE POST to STOREFRONT_REVALIDATE_URL with Bearer STOREFRONT_REVALIDATE_SECRET, 10s AbortController timeout, JSON-parsed response, ok ⇔ HTTP ok AND body.success === true; all failures return { ok:false, message } (missing secret → configuration message, never throws; secret never appears in messages/URLs/logs). NotifyResult exported.
- admin/lib/env.ts: added requireStorefrontRevalidateUrl() (default http://127.0.0.1:3000/api/revalidate) and requireStorefrontRevalidateSecret() (throws per the require* pattern; notify converts that to a failed result).
- admin/lib/data/reviews.ts: setReviewStatus now .select("product_id") and returns the review's product_id (null when 0 rows matched) so moderation actions can build review events; MigrationRequiredError path untouched.
- NEW admin/app/(protected)/notify-actions.ts: retryNotificationAction(prev, formData) — decodes evt (zod-strict; anything invalid rejected), performs EXACTLY ONE new notifyStorefront attempt, sanitizes redirectTo to same-app paths, on ok redirects (redirectTo) or returns a success state, on failure returns the failure state again; never auto-retries. Placed in a dedicated file after checking the existing (protected)/actions.ts (signOut) — no mixing.
- NEW admin/components/NotifyBanner.tsx (client, ui.tsx style): warn-toned alert "«dbMessage» — storefront notification FAILED: <message>" + stale-data note + [Retry Invalidation] form (hidden base64 evt + optional redirectTo); a successful in-form retry swaps the banner for a success message; a failed retry re-renders with the new reason.
- REWROTE admin/app/(protected)/products/actions.ts, categories/actions.ts, reviews/actions.ts: every revalidatePath import/call REMOVED (grep-verified: zero next/cache references left in the admin); notification strictly AFTER DB success; form-state unions extended with { dbSuccess, notify: { failed: true, message }, event, retryRedirectTo? } (reviews' ModerationState = { error } | null | { dbSuccess, notify, event }); redirect()-outside-try/catch discipline, requireSession, zod validation and friendlyDbError preserved exactly.
- Event semantics implemented per spec: product.created (create; dbSuccess state instead of redirect on notify failure, retryRedirectTo=/products/<id>?created=1); product.updated with previousSlug/previousCategoryId captured via getProduct(id) BEFORE the mutation, included only when changed (update); product.updated for archive/activate and all three image actions (redirect-based ?notifyFailed=1&evt=&msg= pattern); product.deleted with slug+previousCategoryId captured pre-delete (failure → /products?deleted=1&notifyFailed=1&evt=… rendered by the LIST page); category.created/updated(previousSlug when changed)/deleted(slug) with the analogous patterns; review.approved/rejected with the productId resolved by setReviewStatus; review.refresh without productId for the whole review domain.
- Manual refresh controls (same single notification path): refreshProductAction on the product detail page next to Archive/Delete, refreshCategoryAction on the category detail page, refreshReviewAction in the reviews page header (preserves status tab + page); all redirect back with ?refreshed=1 (FormMessage success banner) or the notifyFailed pattern on failure.
- UI wiring: ProductForm/CategoryForm render NotifyBanner for the dbSuccess state; ReviewRowActions renders it for in-row moderation failures; product/category/review pages decode evt server-side (invalid → no banner, graceful) and render the retry banner; products/categories LIST pages render the delete-flow retry banner + the previously missing "deleted" success banner.
- Validation: bunx tsc --noEmit = 0 errors; bunx eslint . = 0 problems; runtime codec/notifier tests via mock HTTP server (404 → "storefront rejected the notification (Not found)", 200+success → ok, hanging endpoint → 10s abort, missing secret → config failure with no request, secret never leaked); authenticated render checks with a self-signed session cookie: /admin/login 200, /admin/products 307→login, /admin/categories 307, /admin/reviews 307, and 200 renders of product detail (Revalidate storefront button), notifyFailed banners (Retry Invalidation present) on product/category/reviews pages, list delete-flow banner, refreshed banners, invalid-evt graceful skip, new-product/new-category 200 — zero errors in admin-dev.log. Storefront /api/revalidate currently unreachable (main agent building it in parallel) — handled as the notify-failed path by design.
- OPS: the admin dev server had wedged (TCP accepts stalled ~20 min under the parallel storefront compile load) and the original dev-orchestrator had already died (children orphaned to init). Killed the wedged admin server and restarted the root orchestrator detached (double-fork so it survives the agent shell; idempotent port checks meant the running storefront was skipped, admin restarted on :3001 with .env.local loaded); orchestrator auto-restart capability restored for later crashes. Work record in /home/z/my-project/agent-ctx/3-full-stack-developer.md.

Stage Summary:
- Admin is now a pure domain-change notifier in the Phase 2 architecture: every successful Products/Categories/Reviews mutation emits exactly one frozen-contract event to the storefront revalidation endpoint (10s timeout, no retry/queue/state), failures never roll back the DB and surface "DB success + notification FAILED" with an explicit one-shot [Retry Invalidation], and manual "Revalidate storefront" controls reuse the identical path; all admin-local revalidation removed; 4 files added + 13 modified, all under admin/; tsc + eslint clean; pages compile and render the new states live (end-to-end browser testing with the finished storefront endpoint remains with the main agent, as does the storefront endpoint itself).
- Notable implementation decisions: unpadded base64URL evt encoding (query-safe, isomorphic), an extra urlencoded msg param so the failure reason survives notify-failed redirects (stateless notifier), retry action signature (prev, formData) for useActionState, setReviewStatus returning product_id (null on 0 rows), and a detached dev-orchestrator restart to recover the wedged admin server (storefront process untouched).