1594 upload/Pasted Content_1787120751899.txt
===content===
````markdown id="fusion-phase-5"
# Fusion Gadgets — Phase 5 Implementation Prompt for GLM 5.3

You are continuing the existing Fusion Gadgets codebase after:

```text
Phase 1 — Foundation and Database
Phase 2 — Real Catalog and Mock Data Migration
Phase 3 — Authentication and User Data
Phase 4 — Real Cart and Wishlist
````

Your task in this run is to implement **Phase 5 only: Checkout and COD Order Creation**.

Do not start Phase 6 or any later phase.

The existing premium frontend UI is already designed and approved. Preserve it exactly. Integrate real checkout and order behavior into the existing UI. Do not redesign, simplify, replace, or restyle the website.

Read this entire instruction before changing anything.

First inspect the actual repository and verify the completed previous phases. Do not assume they were implemented perfectly. Verify the real schema, order-related tables, address tables, product data model, offer model, authentication flow, cart implementation, RLS policies, Worker structure, and existing checkout/order UI before changing code.

This phase has a strict authority boundary:

> **The browser never creates an order directly in Supabase. Order creation must go through the Cloudflare Worker.**

Do not bypass this rule.

---

# 1. Phase 5 Objective

Implement the complete real checkout and COD order creation flow.

For now, Fusion Gadgets supports:

```text
COD only
```

Do not add any payment gateway.

Do not add payment abstractions.

Do not add Razorpay, Stripe, PayPal, UPI payment processing, payment webhooks, payment intent tables, or placeholder payment architecture.

The final flow is:

```text
Authenticated User
        │
        ▼
Real Cart
        │
        ▼
Checkout Page
        │
        ▼
Address Required
        │
        ▼
User confirms order
        │
        ▼
Cloudflare Worker
        │
        ├── Authenticate request
        ├── Validate request
        ├── Apply abuse protection
        ├── Load authoritative data
        ├── Revalidate cart products
        ├── Revalidate offers/prices
        ├── Create immutable order snapshot
        ├── Create COD order
        └── Return result
                │
                ▼
Browser
        │
        ▼
Clear/order-update relevant cart state only after success
        │
        ▼
Order confirmation
```

The Worker is the required order authority.

Supabase remains the database and authentication authority.

---

# 2. Strict Scope

Phase 5 includes only:

```text id="ph5scope"
Checkout integration
Authenticated checkout flow
Address selection
Address required validation
Current cart validation
COD-only order creation
Worker-based order creation
Order snapshot creation
Order item snapshot creation
Current product revalidation
Current offer revalidation
Current price calculation
Order totals
Order creation idempotency
Checkout abuse protection
Successful cart cleanup/update
Order confirmation
Real order data integration where required for confirmation
Removal of checkout/order mock simulation
```

Phase 5 does NOT include:

```text id="ph5noscope"
Online payments
Payment gateway
UPI
Razorpay
Stripe
PayPal
Payment webhooks
Refunds
Cancellation workflow
Returns
Inventory reservation
Inventory deduction unless already explicitly required by the real schema
Admin order management
Order fulfillment dashboard
Shipment tracking ingestion
TrackingServer implementation
ProcessingServer implementation
TiDB tracking processing
Order tracking events
Circulation processing
Offer creation/admin UI
```

Do not implement future functionality because order tables already exist.

---

# 3. Mandatory First Step: Audit Before Editing

Before implementation, inspect the actual repository and verify:

```text id="ph5audit"
Phase 1 order schema
Order item schema
Order status model
Order number/identifier strategy
Address schema
Cart schema
Product schema
Offer schema
Current RLS policies
Phase 3 authentication/session flow
Phase 4 real cart behavior
Guest-to-account merge behavior
Existing Worker structure
Existing Worker secrets/configuration
Existing checkout UI
Existing address selection UI
Existing order confirmation UI
Existing order-related mock data
Existing loading states
Existing error states
Existing account/order routes
```

Do not guess table names.

Do not create duplicate order tables.

Do not create a second checkout architecture.

Do not invent an order schema parallel to the Phase 1 foundation.

Use the actual schema.

If a concrete schema defect prevents safe order creation, fix it with the smallest necessary migration.

Do not redesign the entire database.

---

# 4. Final Checkout Architecture

The architecture is fixed.

```text id="ph5architecture"
NEXT.JS FRONTEND
        │
        │ Direct Supabase
        │
        ├── Read own cart
        ├── Read own addresses
        └── Read allowed public product display data
        │
        ▼
USER CONFIRMS CHECKOUT
        │
        ▼
CLOUDFLARE WORKER
        │
        ├── Validate authenticated request
        ├── Validate selected address
        ├── Validate idempotency
        ├── Apply checkout abuse protection
        ├── Read authoritative cart/products/offers
        ├── Calculate authoritative totals
        ├── Create order snapshots
        ├── Create COD order
        └── Finalize result
        │
        ▼
SUPABASE
        │
        ▼
ORDER CREATED
```

The frontend must not create order rows directly.

The frontend must not create order-item rows directly.

The frontend must not calculate a trusted final total.

The frontend may display an estimated/current total, but the Worker is authoritative.

---

# 5. Checkout Requires Authentication

Checkout is not a guest-order system.

The user must be authenticated.

The flow is:

```text id="ph5authcheckout"
User opens checkout
        │
        ▼
Authenticated session?
        │
        ├── NO → redirect to /login
        │
        └── YES → load checkout data
```

Do not create anonymous orders.

Do not create guest checkout.

Do not create a temporary checkout user.

Do not create another authentication flow.

Use the Phase 3 Supabase session.

The browser sends the authenticated request to the Worker using the real user session/token according to the established architecture.

The Worker must independently verify the authenticated identity.

Do not trust a user ID sent in the request body.

---

# 6. Checkout Page Rendering

The checkout page remains aligned with the existing project rendering architecture.

It must not become a server-authenticated page.

The page is:

```text id="ph5shell"
Server-rendered/static shell
        │
        ▼
Hydration
        │
        ▼
Client reads authenticated session
        │
        ▼
Direct Supabase reads own cart/addresses
        │
        ▼
Render authenticated checkout data
```

Do not add server-side auth checks for the page.

Do not fetch the user's cart server-side.

Do not create a Next.js API route.

Do not create Server Actions as a checkout backend.

The interactive checkout content may be client-side.

Keep the server-rendered page shell and static visible layout intact where possible.

Do not place `"use client"` in the page file merely because checkout needs interaction.

---

# 7. Checkout Data Reads

Before placing an order, the frontend may directly read:

```text id="ph5reads"
Own cart
Own addresses
Current public product display data
Current public offer display data where allowed
```

using:

```text
Browser
    │
    ▼
Supabase SDK
    │
    ▼
RLS/public read permissions
```

Do not route ordinary checkout-page reads through the Worker.

The Worker is for the final sensitive order creation.

Do not create a Worker endpoint that merely returns the user's cart.

Do not use the service-role key in browser code.

---

# 8. Address Is Mandatory

A valid address is required before order creation.

The rule is:

```text id="ph5address"
Registration → address optional
Onboarding → address optional
Checkout → address mandatory
```

If the user has no address:

```text
Checkout
    │
    ▼
No valid address available
    │
    ▼
User must add an address before order creation
```

Do not allow order creation without an address.

Do not create a fake address.

Do not silently use a previous/default address without the user's valid selection unless the existing approved UI already has a clear default-address behavior.

The selected address must belong to the authenticated user.

The Worker verifies this independently.

Do not trust an arbitrary address ID from the browser.

---

# 9. Address Snapshot Rule

The order must not depend permanently on the mutable user address record.

When the Worker creates the order:

```text id="ph5addresssnapshot"
Current selected user address
        │
        ▼
Worker verifies ownership
        │
        ▼
Create immutable order address snapshot
        │
        ▼
Order uses snapshot
```

The user must be able to change or delete their profile address later without corrupting historical order information.

Do not simply store a live foreign-key dependency and treat the current mutable address as historical truth if the actual schema supports snapshots.

Use the Phase 1 order snapshot model.

If the snapshot capability is missing and is required for correctness, add the smallest proper schema migration.

Do not duplicate unrelated address infrastructure.

---

# 10. Cart Must Be Valid

Before order creation, the Worker must validate the authoritative cart.

The frontend cart is not authoritative for order creation.

The Worker must load the authenticated user's actual cart state from Supabase.

The Worker validates:

```text id="ph5cartvalidation"
Cart belongs to authenticated user
Cart is not empty
Referenced products exist
Products are currently valid for ordering
Product state is currently eligible
Quantities are valid
```

Do not trust:

```text
Product IDs from frontend alone
Frontend quantity alone
Frontend cart total
Frontend price
Frontend offer calculation
```

The Worker loads the required authoritative records itself.

---

# 11. Product Revalidation

Order creation must use current authoritative product data.

The Worker flow is:

```text id="ph5productvalidation"
Authenticated user
        │
        ▼
Worker loads real cart
        │
        ▼
Worker loads current products
        │
        ▼
For each cart item:
        │
        ├── Product valid?
        ├── Product active/publicly orderable?
        └── Quantity valid?
```

If a product is no longer eligible:

```text id="ph5invalidproduct"
Do not create an order containing stale/fake product data.
```

Return a clear safe failure indicating that checkout data changed and the user must refresh/review the cart.

Do not silently replace the product.

Do not use the price stored in local state.

Do not trust cached product information.

---

# 12. Offer and Price Revalidation

The Worker calculates the authoritative order pricing.

The flow is:

```text id="ph5pricing"
Cart item
        │
        ▼
Current product data
        │
        ▼
Find currently applicable valid offer
        │
        ├── Valid offer → apply actual offer rules
        └── No valid offer → normal current price
        │
        ▼
Calculate authoritative item price
        │
        ▼
Calculate authoritative line total
        │
        ▼
Calculate authoritative order total
```

Do not trust:

```text
Frontend subtotal
Frontend discount
Frontend offer percentage
Frontend final total
Guest/local cached price
Old cart display price
```

The Worker must independently calculate the final result.

Do not create a second custom offer engine if the database/Phase 1 model already defines the required offer rules.

Use the actual offer model.

Do not invent stacking rules.

Do not add coupon support.

Do not add payment discounts.

---

# 13. Money Rules

Use a consistent money representation already compatible with the actual database schema.

Do not use JavaScript floating-point arithmetic carelessly for currency totals.

Do not calculate trusted money values with imprecise float accumulation.

Use the existing schema representation and a reliable deterministic calculation method.

The same authoritative calculated values are saved into the order snapshots.

Do not let the browser provide the final amount.

Do not create a large money abstraction library.

Use the smallest correct implementation.

---

# 14. Immutable Order Snapshot

Once an order is created, historical order data must remain meaningful even if the catalog changes later.

The Worker must create the required immutable snapshots.

The order/item data should preserve the actual information required to understand the completed order, including the relevant product and pricing state at the time of order according to the Phase 1 schema.

Conceptually:

```text id="ph5snapshot"
Current catalog data
        │
        ▼
Worker validates
        │
        ▼
Create order
        │
        ▼
Create immutable order item snapshots
        │
        ├── Product identity/reference
        ├── Product display information required historically
        ├── Quantity
        ├── Applied price
        ├── Applied discount/offer information where applicable
        └── Line total
```

Do not make historical order details depend entirely on today's mutable product title, image, or price.

Do not duplicate excessive product data without purpose.

Use the actual Phase 1 schema.

---

# 15. COD Only

Every order created in this phase is:

```text
COD
```

The order creation request must not accept arbitrary payment methods.

Do not let the frontend submit:

```text
paymentMethod: "anything"
```

and trust it.

The Worker enforces COD-only behavior.

If the schema has a payment method field, set it correctly according to the actual model.

Do not add payment provider integration.

Do not add payment verification.

Do not add payment status machines beyond the existing minimum order/payment state required by the schema.

Do not build future payment architecture.

---

# 16. Worker Order Creation Flow

The final Worker flow must be short, bounded, and deterministic.

A single order creation request should be capable of completing within the intended Worker compute budget.

The Worker must not perform unnecessary sequential work.

The flow is:

```text id="ph5workerflow"
POST /order creation
        │
        ▼
Validate request structure
        │
        ▼
Validate authenticated user
        │
        ▼
Apply lightweight abuse/rate-limit checks
        │
        ▼
Validate idempotency request
        │
        ▼
Load selected address and verify ownership
        │
        ▼
Load authoritative user cart
        │
        ▼
Load required current products/offers efficiently
        │
        ▼
Validate and calculate totals
        │
        ▼
Create order + immutable snapshots
        │
        ▼
Return success
```

Do not add:

```text
Long polling
Waiting for user action
Background loops inside request
Tracking processing
Analytics processing
Notification processing
External payment calls
Multiple unnecessary network round trips
```

The Worker should perform only what is required to safely create the order.

---

# 17. One Request, One Atomic Order Attempt

Order creation must behave as one logical operation.

Do not allow this:

```text id="ph5partial"
Order created
    │
    ├── item snapshot fails
    └── address snapshot fails
```

leaving a broken partial order.

Use the database's actual transactional/RPC capabilities where necessary.

The final write should be atomic from the application's perspective.

Prefer a database-side transaction/function/RPC for multi-table order creation if that is the cleanest reliable way to create the order, items, snapshots, and related state atomically.

The Worker remains the authority boundary.

Do not move authority to the frontend merely because the transaction executes in Supabase.

The flow may be:

```text id="ph5rpc"
Browser
    │
    ▼
Worker
    │
    ▼
Privileged/safe server-side Supabase operation
    │
    ▼
Atomic database transaction/function
```

Use the smallest reliable design compatible with the actual Phase 1 schema and Supabase capabilities.

Do not create a complex microservice architecture.

---

# 18. Service-Role Usage

Privileged database access may be used inside the Worker only where genuinely required for the secure order transaction.

It must never reach:

```text
Browser
Next.js public environment
Client component
Public source code
```

If a database RPC/function can safely perform the required atomic operation with tightly controlled permissions, prefer that over exposing broad direct privileged operations.

Do not expose an unrestricted order-creation RPC directly to unauthenticated clients.

The Worker remains the required request entry point.

Do not weaken database permissions merely to simplify implementation.

---

# 19. Idempotency Is Mandatory

Order creation must not create duplicate orders if:

```text id="ph5idempotencycases"
User double-clicks Place Order
Mobile network retries
Browser retries after uncertain response
Worker response is lost after successful database write
```

The flow must support an idempotency key or equivalent request identity.

The model is:

```text id="ph5idempotencyflow"
Browser generates request identity for one order attempt
        │
        ▼
Send to Worker
        │
        ▼
Worker validates/reuses same logical request
        │
        ├── Already successfully processed
        │      └── Return existing successful result
        │
        └── Not processed
               │
               ▼
           Create one order
```

Do not generate a new order on every retry.

Do not rely solely on disabling the button.

The database must participate in enforcing correctness where appropriate.

Keep idempotency scoped correctly to the authenticated user/order attempt.

Do not create a global idempotency system for the entire application.

---

# 20. Checkout Abuse Prevention

Apply practical protection to order creation.

The Worker must protect against obvious repeated order abuse without overengineering.

Use:

```text id="ph5abuse"
Authenticated identity
Appropriate rate limiting
Idempotency
Strict request validation
Authoritative server-side data validation
```

Do not trust client-side checks.

Do not build invasive device fingerprinting.

Do not claim browser signals are perfect identity.

Do not add external infrastructure merely for checkout abuse prevention.

Do not use TiDB for order abuse state.

Use the simplest practical protection compatible with the existing Worker setup.

Keep the Worker request lightweight.

---

# 21. Order Creation Request

The frontend must send only the minimum information genuinely required.

The browser must not send the entire order as authority.

Do not send:

```text id="ph5requestnono"
Final prices
Final subtotal
Final discount
Final total
Full product snapshots
Trusted cart contents as authority
Arbitrary user ID
Arbitrary order status
Arbitrary payment status
```

The request should contain only the data needed to identify the checkout attempt, such as the selected address and idempotency identity, according to the actual implementation.

The Worker loads the rest authoritatively.

---

# 22. Cart Cleanup After Success

Do not clear the cart before order creation succeeds.

The flow is:

```text id="ph5cartcleanup"
Worker order attempt
        │
        ├── Failure
        │      └── Cart remains
        │
        └── Success
               │
               ▼
        Successful order exists
               │
               ▼
        Remove/clear only the cart items actually ordered
```

Do not clear the entire cart blindly if the final order intentionally contains only specific valid items.

Use the actual order/cart behavior.

For the normal current flow, if the entire authoritative cart is ordered successfully, clear those ordered entries.

The cleanup must not delete unrelated cart changes made concurrently if that is possible.

Do not use a broad "delete all cart rows" command without ensuring it matches the actual successful order contents.

Prefer correctness.

---

# 23. Failure Behavior

If order creation fails:

```text
Do not create fake confirmation.
Do not clear cart.
Do not pretend the order exists.
Do not create partial local orders.
```

Return a safe error.

Keep the cart available for the user to review.

If the failure is caused by stale product/offer data:

```text
Tell the frontend checkout data changed.
Refresh/reload the affected authoritative data.
Allow the user to review before retrying.
```

Do not silently submit a changed order without user awareness.

Do not create an infinite retry loop.

---

# 24. Successful Order Confirmation

After a confirmed successful Worker response:

```text id="ph5success"
Order created
        │
        ▼
Update local cart state to match successful cleanup
        │
        ▼
Navigate/show existing order confirmation UI
        │
        ▼
Display real order identifier/data
```

Do not show a mock order number.

Do not generate a fake frontend confirmation.

Use the real created order result.

Do not immediately create another order if the user refreshes the confirmation page.

Idempotency and the existing order state must prevent duplicate creation.

---

# 25. Order Confirmation Data

Use real order data.

Do not depend on the original checkout state after navigation as the only source of truth.

The confirmation UI must be able to use the real order identity/data according to the existing route structure.

If the existing UI has an order confirmation page, connect it properly.

Do not redesign the route structure without a concrete reason.

Do not create an admin-style order page.

Do not implement full order history beyond the real data integration necessary for confirmation if order-history functionality belongs to a later phase.

Keep this phase focused.

---

# 26. Existing UI Must Stay Intact

Do not redesign:

```text id="ph5uipreserve"
Checkout layout
Address selection UI
Order summary
COD presentation
Place order controls
Loading treatment
Error treatment
Confirmation UI
Mobile layout
Desktop layout
Typography
Spacing
Color system
Motion
```

Connect real behavior to the approved design.

Do not replace it with generic checkout components.

Do not add unnecessary checkout steps.

Do not create a multi-step wizard unless the existing approved UI already is one.

Do not simplify or overcomplicate the UX.

---

# 27. Loading Rules

Preserve the existing global loading architecture.

Do not use the global page progress bar for order creation.

Order placement is an explicit local action.

Use local loading on the actual order action.

The user must not be able to accidentally create multiple concurrent order attempts.

However:

```text
Disabling the button is UX protection.
Idempotency is correctness protection.
```

You need both where appropriate.

Do not add fake delays.

Do not blank the checkout page.

Do not create an unnecessary full-screen loading page.

---

# 28. Server and Client Component Boundaries

Preserve the rendering architecture.

The pattern remains:

```text id="ph5components"
Checkout page
    │
    ├── Server-rendered/static shell
    │
    └── Interactive checkout components
            │
            └── "use client"
```

Do not put `"use client"` in the page file merely because checkout is interactive.

Do not make the entire checkout page one giant Client Component unnecessarily.

Do not create files named only to indicate client status, such as:

```text
CheckoutClient.tsx
OrderClient.tsx
```

Use normal meaningful component names.

Keep static visible content server-rendered where practical.

---

# 29. No Direct Frontend Order Writes

This is absolute.

The frontend must not directly:

```text id="ph5nodirect"
Insert orders
Insert order items
Create order snapshots
Set order totals
Set payment method as authoritative
Set order status
Set payment status
Delete another user's order data
```

Do not use direct Supabase writes as a fallback.

This is forbidden:

```ts id="ph5forbiddenfallback"
try {
  await createOrderThroughWorker(...)
} catch {
  await supabase.from("orders").insert(...)
}
```

No bypass.

If the Worker fails, order creation fails safely.

---

# 30. No Next.js Checkout Backend

Do not create:

```text id="ph5nonextapi"
/app/api/checkout
/app/api/orders
/api/checkout
/api/orders
Server Actions for order creation
Route handlers replacing the Worker
```

Do not move checkout authority into Next.js.

The required architecture is:

```text
Frontend
    │
    ▼
Cloudflare Worker
    │
    ▼
Supabase
```

Keep the Worker separate in:

```text
/worker
```

Do not merge it into the frontend.

---

# 31. No Tracking in This Phase

Do not send order events to:

```text
TrackingServer
ProcessingServer
TiDB
```

Do not implement tracking merely because orders now exist.

Order creation and tracking are separate concerns.

The tracking architecture belongs to later phases.

Do not add event queues.

Do not add analytics processing.

Do not make order creation depend on tracking availability.

---

# 32. No Inventory System in This Phase

Do not invent inventory reservation.

Do not add:

```text
Stock reservation timers
Reservation tables
Cart stock locks
Checkout stock locks
Background reservation cleanup
Inventory microservices
```

Only validate product/order eligibility according to the existing actual product model.

If stock is not yet part of the established business model, do not fabricate it.

Do not overengineer.

---

# 33. Mock Migration

Remove checkout/order simulation as real functionality replaces it.

Audit for:

```text id="ph5mock"
Fake checkout totals
Hardcoded COD result
Mock order IDs
Random order numbers
Simulated successful checkout
Fake order confirmation
Mock address selection
Demo order arrays
Fake processing delays
```

The migration flow is:

```text
Identify mock consumer
        │
        ▼
Connect real implementation
        │
        ▼
Verify real flow
        │
        ▼
Remove obsolete mock code
```

Do not leave production fallback simulation.

Do not show a fake success if the real Worker/database action fails.

---

# 34. RLS and Database Security

Verify that users cannot directly manipulate order data outside the intended boundaries.

The database model must not allow the browser to create arbitrary orders.

The user should only access order data permitted to their authenticated identity and actual Phase 1 policy model.

Do not create broad policies.

Do not expose privileged order writes.

If the Worker uses privileged access or a tightly controlled database function for creation, keep that authority isolated.

Do not weaken RLS merely to make the frontend work.

---

# 35. Type Safety and Validation

Use real TypeScript types aligned with the actual schema.

Do not use `any`.

Validate:

```text id="ph5validation"
Checkout request structure
Selected address identity
Idempotency identity
Authenticated request
Worker input
```

The frontend may validate for UX.

The Worker validates authoritatively.

Do not duplicate large validation systems unnecessarily.

Reuse existing reliable validation utilities if they already exist.

Keep validation explicit and small.

---

# 36. Performance Rules

The Worker must remain efficient.

Avoid unnecessary sequential database requests.

Do not query each product individually if required products can be loaded efficiently together.

Do not perform redundant reads.

Do not call the frontend again to obtain authoritative values.

Do not load unrelated user data.

Do not invoke external services.

Do not perform tracking work.

Do not wait for email or notifications.

Prefer a small bounded request flow.

A single normal order creation request must be designed to complete within the intended Worker compute constraints.

Do not solve simplicity by sacrificing correctness.

Do not solve correctness by adding unnecessary architecture.

---

# 37. Strict No-Drift Rules

The following are absolute.

Do not redesign the UI.

Do not simplify the checkout UI.

Do not add new checkout steps without requirement.

Do not implement guest checkout.

Do not implement online payments.

Do not implement payment gateways.

Do not implement UPI.

Do not add Razorpay.

Do not add Stripe.

Do not add PayPal.

Do not add payment abstractions for future use.

Do not create Next.js API routes.

Do not create Server Actions as an order backend.

Do not move Worker authority into frontend.

Do not merge `/worker` into Next.js.

Do not directly insert orders from browser code.

Do not directly insert order items from browser code.

Do not create a direct Supabase fallback if the Worker fails.

Do not trust frontend totals.

Do not trust frontend prices.

Do not trust frontend discounts.

Do not trust frontend cart data as authoritative.

Do not trust a browser-supplied user ID.

Do not allow order creation without a valid owned address.

Do not create orders without immutable historical snapshots.

Do not create duplicate orders on retry.

Do not rely solely on button disabling for duplicate prevention.

Do not clear the cart before confirmed success.

Do not clear unrelated cart changes blindly.

Do not create fake confirmations.

Do not implement inventory reservation.

Do not implement shipment tracking.

Do not send tracking events.

Do not use TiDB.

Do not implement ProcessingServer logic.

Do not implement circulation.

Do not implement admin functionality.

Do not expose service-role credentials.

Do not weaken RLS.

Do not use `any`.

Do not add unnecessary dependencies.

Do not create unnecessary abstraction layers.

Do not add excessive comments.

Do not write long documentation essays inside source files.

Prefer the shortest secure, reliable implementation that exactly follows this architecture.

---

# 38. Required Verification

Before declaring Phase 5 complete, verify the real implementation with actual seeded products, offers, addresses, and authenticated test users.

Verify:

```text id="ph5verify1"
Unauthenticated user
    ↓
Attempts checkout
    ↓
Cannot create an order
```

```text id="ph5verify2"
Authenticated user
    ↓
No address
    ↓
Cannot place order until valid address exists
```

```text id="ph5verify3"
Authenticated user
    ↓
Selects own address
    ↓
Checkout proceeds
```

```text id="ph5verify4"
Authenticated user
    ↓
Attempts another user's address ID
    ↓
Worker rejects it
```

```text id="ph5verify5"
Worker loads authoritative cart
    ↓
Product data validated
    ↓
Current prices/offers revalidated
    ↓
Frontend total is not trusted
```

```text id="ph5verify6"
Valid COD order
    ↓
One Worker request
    ↓
Order created
    ↓
Order items created
    ↓
Address/product/price snapshots created
    ↓
No partial broken order
```

```text id="ph5verify7"
Same idempotency request retried
    ↓
Only one order exists
    ↓
Existing successful result returned
```

```text id="ph5verify8"
User double-clicks Place Order
    ↓
No duplicate order
```

```text id="ph5verify9"
Product/offer changes before Worker validation
    ↓
Worker uses current authoritative state
    ↓
Stale frontend data is not trusted
```

```text id="ph5verify10"
Order creation fails
    ↓
No fake success
    ↓
Cart remains available
```

```text id="ph5verify11"
Order creation succeeds
    ↓
Only successfully ordered cart entries are cleaned up
    ↓
No unrelated cart state is destroyed
```

```text id="ph5verify12"
Confirmation UI
    ↓
Uses real order identifier/data
    ↓
No mock order number
```

Also verify:

```text id="ph5verifyfinal"
No payment gateway code exists.
No online payment code exists.
No guest checkout exists.
No direct frontend order creation exists.
No Worker bypass/fallback exists.
No Next.js order API route exists.
No Server Action order backend exists.
No privileged credential reaches the browser.
Order creation remains isolated in /worker.
No tracking system was implemented.
No TiDB access was added.
No inventory reservation system was added.
Existing premium UI remains intact.
Static checkout shell architecture remains intact.
Only required components use "use client".
Mock checkout/order infrastructure is removed.
No Phase 6 or later functionality was implemented.
```

Fix every issue found before stopping.

Do not claim completion merely because a button creates a row in the database.

Verify the complete authority boundary, price revalidation, address ownership, atomic order creation, immutable snapshots, idempotency, cart cleanup, and failure behavior.

---

# 39. Required Completion Output

When Phase 5 is genuinely complete, report only:

```text
Phase 5 completed

Implemented:
- ...

Checkout flow:
- ...

COD order flow:
- ...

Worker authority:
- ...

Authoritative validation:
- ...

Order snapshots:
- ...

Idempotency verified:
- ...

Cart cleanup behavior:
- ...

Security/RLS boundaries verified:
- ...

Mock infrastructure removed:
- ...

Files/folders added or changed:
- ...

Architecture boundaries preserved:
- ...

Not implemented because it belongs to later phases:
- ...
```

Do not start Phase 6.

Do not continue implementing after this report.

```
```